#统一的配置类,千年不动的配置放到这里
#spring的配置类
spring:
  application:
    name: hhh-consumer-admin
  config:
    name: application,hystrix
  profiles:
    active:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    joda-date-time-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8




