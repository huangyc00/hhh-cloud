#统一的配置类,千年不动的配置放到这里
#spring的配置类
spring:
  application:
    name: hhh-consumer-minipro
  config:
    name: application,hystrix
  profiles:
    active: dev
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    joda-date-time-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8




